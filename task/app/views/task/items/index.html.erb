<p id="notice"><%= notice %></p>

<h1>Items</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Status</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.name %></td>
        <td><%= item.status %></td>
        <td><%= item.user_id %></td>
        <td><%= link_to 'Show', item_path(item.id) %></td>
        <td><%= link_to 'Edit', edit_item_path(item.id) %></td>
        <td><%= link_to 'Destroy', item_path(item.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Item', new_item_path %>
